# Optional dependencies omitted by default
#omit = ["optional"]

# Install-script approvals are pinned in the root package.json `allowScripts`.
# Do not add workspace-level allow-scripts entries; npm ignores them.

# (Optional) personal defaults
#save-exact=true
#loglevel=warn
